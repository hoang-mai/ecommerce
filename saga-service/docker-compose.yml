version: '3.8'
services:
  saga-service:
    image: maianhhoang31072003/saga-service:latest
    container_name: saga-service
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "8088:8088"


