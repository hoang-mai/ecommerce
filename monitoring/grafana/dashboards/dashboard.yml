apiVersion: 1

providers:
  - name: "spring"
    folder: "Spring Boot"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboard-files
